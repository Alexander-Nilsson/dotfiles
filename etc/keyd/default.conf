[ids]
*

[main]
# Tap = Esc, Hold = Ctrl
capslock = overload(control, esc)

# Tap = < > | key, Hold = nav layer
102nd = overloadt(nav, 102nd, 200)

# Disable physical arrow keys
left  = noop
right = noop
up    = noop
down  = noop

[nav]
h = left
j = down
k = up
l = right
